import React from 'react'; 
import "./list.css"; 

const list = (props) => {
    return (
        <div className="listContainer">
            List of channels
        </div>
    );

};

export default list; 